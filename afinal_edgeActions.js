/***********************
* Adobe Edge Animate Composition Actions
*
* Edit this file with caution, being careful to preserve 
* function signatures and comments starting with 'Edge' to maintain the 
* ability to interact with these actions from within Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // aliases for commonly used Edge classes

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
      Symbol.bindElementAction(compId, symbolName, "${_MASK}", "click", function(sym, e) {
         sym.$("MASK_Question").show();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_FATHER}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER_Question").show();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_MOTHER}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER_Question").show();

      });
      //Edge binding end

      

      

      

      

      

      

      

      

      

      Symbol.bindElementAction(compId, symbolName, "${__2human}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("peop2").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE_Question").hide();
         

      });
      //Edge binding end

      

      Symbol.bindElementAction(compId, symbolName, "${_brest}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("mask2").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK_Question").hide();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_butter2}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("mask2").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK_Question").hide();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_vagCopy2}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("mask2").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK_Question").hide();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_caveCopy2}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("mask1").show();
         // insert code for mouse click here// Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK_Question").hide();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_clownCopy4}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("mask1").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK_Question").hide();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_clownCopy3}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("mask3").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK_Question").hide();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_amask}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("mask1").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK_Question").hide();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Twoboots}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("fath1").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER_Question").hide();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Maleorgans}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("fath1").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER_Question").hide();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_gorilla}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("fath2").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER_Question").hide();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_bear}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("fath2").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER_Question").hide();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_worm}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("fath1").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER_Question").hide();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_PEOPLE}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE_Question").show();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_butter}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("peop1").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE_Question").hide();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_vag}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("peop1").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE_Question").hide();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_cave}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("peop1").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE_Question").hide();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_clown}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("peop1").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE_Question").hide();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_twofaces}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("moth3").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER_Question").hide();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_ears}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("moth3").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER_Question").hide();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_witches}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("moth2").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER_Question").hide();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_girls}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("moth2").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER_Question").hide();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_lamp}", "click", function(sym, e) {
         // Show an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("moth1").show();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER").hide();
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER_Question").hide();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Close1}", "click", function(sym, e) {
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("PEOPLE_Question").hide();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Close2}", "click", function(sym, e) {
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MASK_Question").hide();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Close3}", "click", function(sym, e) {
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("FATHER_Question").hide();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Close4}", "click", function(sym, e) {
         // Hide an Element.
         //  (sym.$("name") resolves an Edge Animate element name to a DOM
         //  element that can be used with jQuery)
         sym.$("MOTHER_Question").hide();
         
         

      });
      //Edge binding end

   })("stage");
   //Edge symbol end:'stage'

   //=========================================================

   //=========================================================
   
   //Edge symbol: '_EF_Base_Adobe_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 0, function(sym, e) {
         sym.stop();

      });
         //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1250, function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play('active');

      });
         //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_BaseCopy}", "mouseover", function(sym, e) {
         sym.play('active');

      });
         //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_BaseCopy}", "mouseout", function(sym, e) {
         sym.play('inactive');

      });
         //Edge binding end

      })("_EF_BaseT_Adobe");
   //Edge symbol end:'_EF_BaseT_Adobe'

   //=========================================================
   
   //Edge symbol: 'EF_Textbox_Adobe'
   (function(symbolName) {   
   
      Symbol.bindSymbolAction(compId, symbolName, "creationComplete", function(sym, e) {
         var ele = sym.getSymbolElement();
         var title = ele.attr("title");
         var spc = $('#'+ele.attr("id")+'__EF_SpaceT')
         spc.html("<textarea form='efform' id='"+title+"' name='"+title+"'></textarea>");
         $("<style type='text/css'> .ef_tbox { padding-left: 6px; padding-right: 6px; color: rgb(192,192,192); background-color: rgba(0,0,0,0); border: none; height: 100%; outline: none; } #.ef_tbox:focus { border: none; } </style>").appendTo("head");
         $('#'+title).addClass("ef_tbox");
         $('#'+title).css('width',spc.width()-12);
         var base = sym.getSymbol('_EF_BaseT_Adobe');
         $('#'+title).mouseenter(function() { base.play('active') });
         $('#'+title).mouseleave(function() { base.play('inactive') });

      });
      //Edge binding end

   })("EF_Textbox_Adobe");
   //Edge symbol end:'EF_Textbox_Adobe'

   //=========================================================
   
   //Edge symbol: '_EF_BaseB_Adobe'
   (function(symbolName) {   
   
      Symbol.bindElementAction(compId, symbolName, "${__EF_BaseB_Adobe}", "click", function(sym, e) {
         p = sym.getSymbolElement().parent();
         //p.submit();

      });
         //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1000, function(sym, e) {
         sym.play('active');

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 124, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      

   })("_EF_BaseB_Adobe");
   //Edge symbol end:'_EF_BaseB_Adobe'

   //=========================================================
   
   //Edge symbol: '_EF_BaseR_Adobe'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 0, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1250, function(sym, e) {
         sym.play('active');

      });
      //Edge binding end

   })("_EF_BaseR_Adobe");
   //Edge symbol end:'_EF_BaseR_Adobe'

   //=========================================================
   
   //Edge symbol: 'EF_Checkbox_Adobe'
   (function(symbolName) {   
   
      Symbol.bindSymbolAction(compId, symbolName, "creationComplete", function(sym, e) {
         //form='efform' 
         var title = sym.getSymbolElement().attr("title");
         ele = sym.getSymbolElement();
         ele.append("<input form='efform' id='"+title+"' name='"+title+"' type='hidden' value='off' />");
         ele.click(function() {
         	if(sym.getPosition() > 0) {
         		sym.play('turn_off');
         		$('#'+title).val("off");
         	} else {
         		sym.play('turn_on');
         		$('#'+title).val("on");
         	}
         });
         var shad = sym.getSymbol("_EF_ShadowC_Adobe");
         sele = shad.getSymbolElement();
         ele.mouseenter(function() { shad.play('active'); });
         ele.mouseleave(function() { shad.play('inactive'); });

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1500, function(sym, e) {
         sym.stop('turn_on');

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 750, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

   })("EF_Checkbox_Adobe");
   //Edge symbol end:'EF_Checkbox_Adobe'

   //=========================================================
   
   //Edge symbol: 'EF_Input_Adobe_1'
   (function(symbolName) {   
   
      Symbol.bindSymbolAction(compId, symbolName, "creationComplete", function(sym, e) {
         var ele = sym.getSymbolElement();
         var title = ele.attr("title");
         var spc = $('#'+ele.attr("id")+'__EF_SpaceI')
         spc.html("<input form='efform' type='password' id='"+title+"' name='"+title+"' />");
         $("<style type='text/css'> .ef_input { padding-left: 6px; padding-right: 6px; color: rgb(192,192,192); background-color: rgba(0,0,0,0); border: none; height: 100%; outline: none; } #.ef_input:focus { border: none; } </style>").appendTo("head");
         $('#'+title).addClass("ef_input");
         $('#'+title).css('width',spc.width()-12);
         var base = sym.getSymbol('_EF_Base_AdobeI');
         $('#'+title).mouseenter(function() { base.play('active') });
         $('#'+title).mouseleave(function() { base.play('inactive') });

      });
      //Edge binding end

   })("EF_Password_Adobe");
   //Edge symbol end:'EF_Password_Adobe'

   //=========================================================
   
   //Edge symbol: 'EF_Radio_Adobe'
   (function(symbolName) {   
   
      Symbol.bindSymbolAction(compId, symbolName, "creationComplete", function(sym, e) {
         var ele = sym.getSymbolElement();
         var pairs = ele.attr("title").split("-");
         var title = pairs[0];
         var mkr = sym.getSymbol('_EF_Marker_Adobe');
         mkr.setVariable("optval",pairs[1]);
         var mkre = mkr.getSymbolElement();
         if($('#'+title).length <= 0)
         	ele.append("<input form='efform' id='"+title+"' name='"+title+"' type='hidden' value='' />");
         var base = sym.getSymbol('_EF_BaseR_Adobe');
         mkre.mouseenter(function() { base.play('active') });
         mkre.mouseleave(function() { base.play('inactive') });
         mkre.click(function() {
         	children = sym.getComposition().getStage().getChildSymbols();
         	for(var i=0; i<children.length; i++)
         	{
         		if(children[i].getSymbolElement().attr("title").split("-")[0] == title)
         			if(children[i].getSymbol('_EF_Marker_Adobe').getPosition() > 0)
         				children[i].getSymbol('_EF_Marker_Adobe').playReverse('on');
         	}
         	mkr.play('off');
         	$('#'+title).val(mkr.getVariable("optval"));
         });

      });
      //Edge binding end

   })("EF_Radio_Adobe");
   //Edge symbol end:'EF_Radio_Adobe'

   //=========================================================
   
   //Edge symbol: '_EF_Base_Adobe'
   (function(symbolName) {   
   
      Symbol.bindElementAction(compId, symbolName, "${_BaseCopy}", "mouseover", function(sym, e) {
         sym.play('active');

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_BaseCopy}", "mouseout", function(sym, e) {
         sym.play('inactive');

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 0, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1250, function(sym, e) {
         // play the timeline from the given position (ms or label)
         sym.play('active');

      });
      //Edge binding end

   })("_EF_Base_Adobe");
   //Edge symbol end:'_EF_Base_Adobe'

   //=========================================================
   
   //Edge symbol: '_EF_Marker_Adobe'
   (function(symbolName) {   
   
   })("_EF_Marker_Adobe");
   //Edge symbol end:'_EF_Marker_Adobe'

   //=========================================================
   
   //Edge symbol: 'Symbol_1'
   (function(symbolName) {   
   
      

      Symbol.bindElementAction(compId, symbolName, "${__EF_ShadowB_Adobe}", "click", function(sym, e) {
         p = sym.getSymbolElement().parent();
         //p.submit();

      });
      //Edge binding end

      Symbol.bindSymbolAction(compId, symbolName, "creationComplete", function(sym, e) {
         var title = sym.getSymbolElement().attr("title");
         sym.$("_EF_ButtonLabel").html(title);
         sym.$("_EF_ButtonLabel").prepend("<form id='efform' method='get' action='index.html'></form>");
         var base = sym.getSymbol("_EF_BaseB_Adobe");
         var sele = sym.$("_EF_ButtonLabel");
         sele.mouseenter(function() { base.play('active'); });
         sele.mouseleave(function() { base.play('inactive'); });
         sele.click(function() {
         	$("#efform").submit();
         });

      });
      //Edge binding end

   })("EF_Button_Adobe");
   //Edge symbol end:'EF_Button_Adobe'

   //=========================================================
   
   //Edge symbol: '_EF_Base_Adobe_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 250, function(sym, e) {
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1250, function(sym, e) {
         sym.play('active');

      });
      //Edge binding end

      })("_EF_ShadowC_Adobe");
   //Edge symbol end:'_EF_ShadowC_Adobe'

   //=========================================================
   
   //Edge symbol: 'EF_Input_Adobe'
   (function(symbolName) {   
   
      Symbol.bindSymbolAction(compId, symbolName, "creationComplete", function(sym, e) {
         var ele = sym.getSymbolElement();
         var title = ele.attr("title");
         var spc = $('#'+ele.attr("id")+'__EF_SpaceI')
         spc.html("<input form='efform' id='"+title+"' name='"+title+"' />");
         $("<style type='text/css'> .ef_input { padding-left: 6px; padding-right: 6px; color: rgb(192,192,192); background-color: rgba(0,0,0,0); border: none; height: 100%; outline: none; } #.ef_input:focus { border: none; } </style>").appendTo("head");
         $('#'+title).addClass("ef_input");
         $('#'+title).css('width',spc.width()-12);
         var base = sym.getSymbol('_EF_Base_AdobeI');
         $('#'+title).mouseenter(function() { base.play('active') });
         $('#'+title).mouseleave(function() { base.play('inactive') });

      });
      //Edge binding end

   })("EF_Input_Adobe");
   //Edge symbol end:'EF_Input_Adobe'

   //=========================================================
   
   //Edge symbol: 'Close1'
   (function(symbolName) {   
   
   })("Close1");
   //Edge symbol end:'Close1'

})(jQuery, AdobeEdge, "EDGE-3120272267");